Feature 5: Logging System
- Creates or appends to a log file
- Adds timestamps to every entry
- Stores results from GET, POST, validation, and response-time tools
- Supports the Logging System (Feature 5)

